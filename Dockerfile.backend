# Backend service image for Debate Club
FROM node:20-alpine AS base

WORKDIR /app

COPY package*.json ./
RUN if [ -f package.json ]; then npm install --only=production; fi

COPY . .

EXPOSE 4000

CMD ["npm", "run", "start:backend"]
